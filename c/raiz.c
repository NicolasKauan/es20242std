#include <stdio.h>
#include <math.h>

int main (){
    double x, resultado;
    printf ("Informe um numero:\n");
    scanf("%lf",&x);
    resultado = ((-1)*sqrt(x));
    printf("a raiz quadrada e %.2lf",resultado);
    
    
    
    
    
    
    return 0;
}